
  # Design CampusSync Faculty Module

  This is a code bundle for Design CampusSync Faculty Module. The original project is available at https://www.figma.com/design/hbT0PeEChBQHhVNMm1vdTK/Design-CampusSync-Faculty-Module.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # faculty-finder-updated
